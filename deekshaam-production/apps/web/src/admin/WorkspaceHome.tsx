import React from 'react';
import { Icon } from '@deekshaam/ui';
import { workspaceModules, canAccess, roleLabels } from './access';
export const WorkspaceHome: React.FC<{ user: any; onSelectTab: (tab: string) => void }> = ({ user, onSelectTab }) => <div><div className="workspace-page-heading"><span className="eyebrow">{roleLabels[user.role]}</span><h1>Welcome, {user.name}.</h1><p>Your tools for supporting students and keeping Deekshaam moving.</p></div><div className="workspace-shortcuts">{workspaceModules.filter(m => m.id !== 'dashboard' && canAccess(user.role, m.id)).map(item => <button className="admin-card workspace-shortcut" key={item.id} onClick={() => onSelectTab(item.id)}><Icon name={item.icon} size={24} /><h2>{item.label}</h2><p>{item.description}</p><span className="text-link">Open workspace <Icon name="arrow" size={14} /></span></button>)}</div><div className="workspace-help"><h2>Need access to another area?</h2><p>Your administrator assigns permissions to your account. Contact them if your responsibilities change.</p></div></div>;
